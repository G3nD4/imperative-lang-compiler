routine IsPrime(n : integer): boolean is
    if n <= 1 then
        return false
    end
    for divisor in 2..(n - 1) loop
        if n % divisor = 0 then
            return false
        end
    end
    return true
end

for i in 1..100 loop
    if IsPrime(i) then
        print(i)
    end
end